name: Valgrind

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  NGINX_VERSION: "1.31.1"

on:
  schedule:
    # Monthly, 1st @ 03:30 UTC.
    - cron: "30 3 1 * *"
  workflow_dispatch: {}

concurrency:
  group: valgrind-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  memcheck:
    name: Memcheck
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential curl libpcre2-dev libssl-dev valgrind zlib1g-dev

      - name: Cache source tarball
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: .build/nginx-${{ env.NGINX_VERSION }}.tar.gz
          key: src-nginx-${{ env.NGINX_VERSION }}

      - name: Build debug server and module
        run: bash tools/ci-build.sh nginx "$NGINX_VERSION" debug

      - name: Run runtime suite under Memcheck
        run: |
          runner="valgrind --tool=memcheck --track-origins=yes --leak-check=full"
          runner="$runner --show-leak-kinds=definite,indirect"
          runner="$runner --errors-for-leak-kinds=definite,indirect --error-exitcode=99"
          runner="$runner --suppressions=$GITHUB_WORKSPACE/tools/valgrind.supp"
          runner="$runner --log-file=$GITHUB_WORKSPACE/valgrind-%p.log"
          python3 tools/test_runtime.py \
            --single-process \
            --runner "$runner" \
            --nginx-binary "$GITHUB_WORKSPACE/.build/nginx-${NGINX_VERSION}/objs/nginx" \
            --module "$GITHUB_WORKSPACE/.build/nginx-${NGINX_VERSION}/objs/ngx_http_sentinel_module.so"

      - name: Verify Memcheck reports
        if: always()
        run: |
          cat valgrind-*.log || true
          if grep -Eq 'ERROR SUMMARY: [1-9][0-9]* errors' valgrind-*.log; then
            exit 1
          fi
          grep -Eq 'ERROR SUMMARY: 0 errors' valgrind-*.log

      - name: Upload Memcheck reports
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: valgrind-reports
          path: valgrind-*.log
          retention-days: 14
